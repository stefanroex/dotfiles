---
- name: Homebrew casks - Install
  homebrew_cask:
    name: "{{ item }}"
    state: installed
  ignore_errors: true
  loop:
  - 1password
  - aws-vault
  - chromedriver
  - firefox
  - google-chrome
  - raycast
  - rectangle
  - slack
  - spotify
  - transmission
  - tunnelblick
