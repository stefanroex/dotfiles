chpwd() {
  ls
}

c() { cd ~/code/$1; }
_c() { _files -W ~/code -/; }
compdef _c c

function mcd() {
  mkdir -p "$1" && cd "$1";
}

function herokus() {
  heroku "$@" --remote staging
}

function herokup() {
  heroku "$@" --remote production
}
